<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="dev.gitly.viewmodel.AuthState" />

        <import type="dev.gitly.core.prefs.AuthPrefs" />

        <import type="dev.gitly.viewmodel.AuthViewModel" />

        <import type="dev.gitly.model.data.User" />

        <import type="android.view.View" />

        <variable
            name="authState"
            type="AuthState" />

        <variable
            name="prefs"
            type="AuthPrefs" />

        <variable
            name="viewModel"
            type="AuthViewModel" />

        <variable
            name="email"
            type="String" />

        <variable
            name="password"
            type="String" />

        <variable
            name="currentUser"
            type="User" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="@dimen/spacing_xxlarge"
        tools:context=".view.AuthFragment">

        <TextView
            android:id="@+id/login_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xxxlarge"
            android:gravity="center"
            android:paddingVertical="@dimen/spacing_large"
            android:text="@string/app_name"
            android:textAppearance="?textAppearanceHeadline5"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/login_helper"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_normal"
            android:gravity="center"
            android:text="@string/login_header"
            android:textAppearance="?textAppearanceSubtitle2"
            app:layout_constraintTop_toBottomOf="@id/login_title" />

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/email_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_large"
            android:hint="@string/email"
            android:visibility="@{prefs.token == null ? View.VISIBLE : View.GONE, default=`visible`}"
            app:layout_constraintTop_toBottomOf="@id/login_helper">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/user_email"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:enabled="@{prefs.token == null, default=`true`}"
                android:inputType="textEmailAddress"
                tools:text="gitly@domain.com" />

        </com.google.android.material.textfield.TextInputLayout>

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/password_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_large"
            android:hint="@string/password"
            android:visibility="@{prefs.token == null ? View.VISIBLE : View.GONE, default=`visible`}"
            app:counterEnabled="true"
            app:counterMaxLength="25"
            app:layout_constraintTop_toBottomOf="@id/email_container"
            app:passwordToggleEnabled="true">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/user_password"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:enabled="@{prefs.token == null, default=`true`}"
                android:inputType="textPassword"
                tools:text="@tools:sample/full_names" />

        </com.google.android.material.textfield.TextInputLayout>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_normal"
            android:enabled="@{authState != AuthState.AUTHENTICATING, default=`true`}"
            android:onClick="@{() -> viewModel.getAccessToken(email, password)}"
            android:text="@string/login"
            android:visibility="@{prefs.token == null ? View.VISIBLE : View.GONE, default=`visible`}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/password_container" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xxlarge"
            android:onClick="@{() -> viewModel.viewCurrentUserProfile()}"
            android:text="@string/get_user"
            android:visibility="@{prefs.token != null? View.VISIBLE : View.GONE, default=`gone`}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/password_container" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>